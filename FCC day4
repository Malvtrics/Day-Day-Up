1: \ in string is meaningless, should use \\ instead
2: indexOf can be used for string and array
3: charCodeAt()  fromCharCode() very useful
4: Object.keys(val) good method for parsing json data
5: pre-filter json data:
    json = json.filter(function(val) {
      return (val.id !== 1);
    });
6: get user's position 
if (navigator.geolocation) {
  navigator.geolocation.getCurrentPosition(function(position) {
    $("#data").html("latitude: " + position.coords.latitude + "<br>longitude: " + position.coords.longitude);
  });
}
7:
